<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SocialLink | Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col items-center p-10">
    <!-- Top Right Links -->
    <div class="absolute top-4 right-4 flex gap-4">
        <a href="/profile" class="bg-blue-500 text-white px-4 py-2 rounded-md shadow-lg hover:bg-blue-600 transition duration-300">Profile</a>
        <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded-md shadow-lg hover:bg-red-600 transition duration-300">Logout</a>
    </div>

    <!-- Main Content -->
    <div class="w-full max-w-2xl bg-zinc-800 p-10 rounded-lg shadow-lg">
        <h3 class="text-3xl font-bold mb-6 text-blue-300 text-center">Edit Your Post</h3>
        <form action="/update/<%=post._id%>" method="post" class="space-y-4">
            <textarea name="content" placeholder="What's on your mind?" class="block w-full px-3 py-2 border-2 border-zinc-600 rounded-md bg-transparent text-white focus:border-blue-500 resize-none h-40"><%=post.content%></textarea>
            <input type="submit" class="w-full px-5 py-2 rounded-md bg-yellow-500 text-center text-white font-semibold cursor-pointer hover:bg-yellow-600 transition duration-300 shadow-lg" value="Update Post">
        </form>
    </div>
</body>
</html>
