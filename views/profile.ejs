<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10 bg-red-500 rounded-md overflow-hidden">
                <img src="/images/uploads/<%=user.profilepic%>" alt="" class="w-full h-full object-cover">
            </div>
            <div class="flex gap-20 items-center">
                <h3 class="text-3xl mb-5">
                    <%= user.name%>
                </h3>
                <a href="/profile/upload" class="font-medium text-blue-600 dark:text-blue-500 hover:underline">Upload
                    Photo</a>
            </div>

        </div>
        <h5 class="mb-5">You can create a new post</h5>
        <form action="/profile" method="post">
            <textarea name="content" placeholder="What's on your mind?"
                class=" block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md block"></textarea>
            <input type="submit" class="w-40 px-3 py-2 bg-blue-500 rounded-md mt-2 text-sm" value="Create new post">
        </form>

        <div class="posts mt-10">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 mb-4 border-2 border-zinc-800 rounded-md">
                        <h4 class="text-blue-500 mb-2">
                            <%= user.username%>
                        </h4>
                        <p class="text-sm tracking tight">
                            <%=post.content%>
                        </p>
                        <small class="mt-2 inline-block">
                            <%=post.likes.length %> Likes
                        </small>
                        <div class="btns flex gap-4 mt-5">
                            <a href="/like/<%=post._id%>" class="text-blue-500">
                                <%= post.likes.indexOf(user._id) !==-1 ? "Unlike" : "Like" %>
                            </a>
                            <a href="/edit/<%=post._id%>" class="text-zinc-600">Edit</a>
                        </div>
                    </div>
                    <%}) %>

            </div>
        </div>
    </div>
</body>

</html>