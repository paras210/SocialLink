<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SocialLink | Upload Profile Picture</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gradient-to-br from-gray-900 via-black to-gray-900 text-white min-h-screen flex flex-col items-center justify-center p-8 relative">

    <!-- Top Right Links -->
    <div class="absolute top-8 right-8 flex gap-4">
        <a href="/profile" class="bg-blue-500 text-white px-4 py-2 rounded-md shadow-lg hover:bg-blue-600 transition duration-300">Go to Profile</a>
        <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded-md shadow-lg hover:bg-red-600 transition duration-300">Logout</a>
    </div>

    <!-- Form Container -->
    <div class="bg-zinc-800 p-6 rounded-lg shadow-lg w-full max-w-sm">
        <h3 class="text-3xl font-bold mb-6 text-blue-300 text-center">Upload Profile Picture</h3>
        <form action="/upload" autocomplete="off" method="post" enctype="multipart/form-data" class="space-y-4">
            <input type="file" name="image" class="block w-full px-3 py-2 border-2 border-zinc-600 rounded-md bg-transparent text-white focus:border-blue-500 transition duration-300">
            <input type="submit" class="w-full px-5 py-2 rounded-md bg-blue-500 cursor-pointer text-white font-semibold hover:bg-blue-600 transition duration-300 shadow-lg" value="Upload Image">
        </form>
    </div>
</body>
</html>
